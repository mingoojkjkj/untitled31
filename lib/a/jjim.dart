import 'package:flutter/material.dart';

class jjim extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('jjim'

      ),),
      body: Center(
        child:  Text('jjim'),
      ),
    );
  }
}

